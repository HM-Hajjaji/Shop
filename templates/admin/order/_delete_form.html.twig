<form method="post" action="{{ path('app_order_delete', {'slug': order.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order.slug) }}">
    <button class="btn">Delete</button>
</form>
