    {% block block_action %}
        <a class="btn btn-info btn" href="{{path('app_product_edit', {'slug': product.slug}) }}">
            <i class="fas fa-pencil-alt">
            </i>
            Edit
        </a>
    {% endblock %}
    <a class="btn btn-danger btn-sm" href="#">
        {{ include('admin/product/_delete_form.html.twig',{'slug' : product.slug}) }}
    </a>